<view class="container">
	<!-- 用户登录测试 -->
	<view class="userinfo" bindtap="login">
		<image class="userinfo-avatar" src="{{logged ? userInfo.avatarUrl : './user-unlogin.png'}}" background-size="cover"></image>
		<view>
			<text class="userinfo-nickname">{{logged ? userInfo.nickName : '登录'}}</text>
			<text class="userinfo-nickname" wx:if="{{logged}}">{{userInfo.lastLoginTime}}</text>
		</view>
	</view>

	<view class="uploader">
		<navigator url="../mr/new" open-type="navigate" class="uploader-text">
			<text>新增病历</text>
		</navigator>
	</view>

	<!-- 病历列表 -->
	<view class="uploader">
		<navigator url="../mr/index" open-type="navigate" class="uploader-text">
			<text>病历列表</text>
		</navigator>
	</view>
</view>
